package com.syntax.class13;

import utils.StringUtils;

public class StringMethods2 {
    public static void main(String[] args) {

        String str="Thor love and thunder";

// Let say I want to find out the indexes of all occurrences of a character, we can use a loop that can "go through"(iterate) all the characters
           //   for (int i=0; i<str.length(); i++){
            //      if (str.charAt(i)=='o') {
            //          System.out.println(i);
             //     }
              //    }
        }

// If we need this over and over, it means we will continue to create the above logic anytime we need it
    // An easier way will be to create a method that we can call any time we need to get the indexes

            StringUtils strUtils=new StringUtils();
           // strUtils.printAllIndexes(str,'e');











    }

